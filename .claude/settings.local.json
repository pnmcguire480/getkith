{
  "permissions": {
    "allow": [
      "Bash(unzip -o files.zip)",
      "Bash(unzip -l kith-deploy.zip)",
      "Bash(unzip -o kith-deploy.zip -d kith-deploy)",
      "Bash(git init:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)"
    ]
  }
}
